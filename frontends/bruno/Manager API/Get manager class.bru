meta {
  name: Get manager class
  type: http
  seq: 2
}

get {
  url: http://{{server_address}}:{{server_port}}/api/v1/manager/{{manager_id}}
  body: none
  auth: inherit
}

body:json {
  {
      "prompt": "Your text to analyze here",
      "pipeline_flow": "code_audit"
  }
}

vars:pre-request {
  manager_id: llm
}

settings {
  encodeUrl: true
  timeout: 0
}
